apiVersion: v1
kind: Service
metadata:
  name: insurance-agent
  labels:
    app: insurance-agent
spec:
  selector:
    app: insurance-agent
  ports:
    - name: http
      port: 8080
      targetPort: 7860
      protocol: TCP
  type: ClusterIP
